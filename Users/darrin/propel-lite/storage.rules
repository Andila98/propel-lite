
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to all files for now
    // In a production app, you should secure your storage
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
