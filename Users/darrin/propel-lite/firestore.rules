
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for now
    // In a production app, you should secure your database
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
